{
  "version": 2,
  "framework": null,
  "nodeVersion": "22.x",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/creatortasks run build",
  "outputDirectory": "artifacts/creatortasks/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" }
  ]
}
