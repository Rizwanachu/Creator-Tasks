1. CRITICAL FIX (ESCROW LOGIC)

Right now you wrote:

creator pays budget from their balance

But you don’t yet have deposits.

👉 So for MVP:

DO THIS INSTEAD:

Allow task creation WITHOUT real balance deduction
Treat budget as “virtual escrow”

Otherwise:

You’ll block users instantly
Nobody can post tasks
✅ Correct MVP Logic

When task is created:

Store budget in task
DO NOT deduct from user balance yet

When approved:

Worker gets 90%
Platform logs 10%

That’s enough for MVP validation.

🧱 2. DRIZZLE SCHEMA (READY-TO-USE)

Use this directly:

import { pgTable, text, integer, uuid, timestamp } from "drizzle-orm/pg-core";

export const users = pgTable("users", {
  id: uuid("id").defaultRandom().primaryKey(),
  clerkId: text("clerk_id").notNull().unique(),
  email: text("email").notNull(),
  name: text("name"),
  balance: integer("balance").default(0),
  pendingBalance: integer("pending_balance").default(0),
});

export const tasks = pgTable("tasks", {
  id: uuid("id").defaultRandom().primaryKey(),
  title: text("title").notNull(),
  description: text("description").notNull(),
  budget: integer("budget").notNull(),
  status: text("status").default("open"), // open, in_progress, submitted, completed
  creatorId: uuid("creator_id").notNull(),
  workerId: uuid("worker_id"),
  createdAt: timestamp("created_at").defaultNow(),
});

export const submissions = pgTable("submissions", {
  id: uuid("id").defaultRandom().primaryKey(),
  taskId: uuid("task_id").notNull(),
  content: text("content").notNull(),
  status: text("status").default("pending"), // pending, approved, rejected
});

export const transactions = pgTable("transactions", {
  id: uuid("id").defaultRandom().primaryKey(),
  userId: uuid("user_id").notNull(),
  amount: integer("amount").notNull(),
  type: text("type").notNull(), // earning, fee
  createdAt: timestamp("created_at").defaultNow(),
});
🔥 3. MOST IMPORTANT API (APPROVE FLOW)

This is your money engine — don’t mess this up.

// POST /api/tasks/:id/approve

// 1. Get task
// 2. Ensure requester = creator
// 3. Ensure status = submitted

const workerEarning = Math.floor(task.budget * 0.9);
const platformFee = task.budget - workerEarning;

// 4. Update worker balance
await db.update(users)
  .set({
    balance: sql`balance + ${workerEarning}`
  })
  .where(eq(users.id, task.workerId));

// 5. Log transactions
await db.insert(transactions).values([
  {
    userId: task.workerId,
    amount: workerEarning,
    type: "earning",
  },
  {
    userId: task.creatorId,
    amount: platformFee,
    type: "fee",
  }
]);

// 6. Update task
await db.update(tasks)
  .set({ status: "completed" })
  .where(eq(tasks.id, task.id));
⚠️ 4. BUG YOU WILL HIT (and fix now)
Problem:

Two users click “Accept Task” at same time

Fix:

Add condition in DB update:

where:
  id = taskId
  AND status = 'open'

If update fails → task already taken.

🎯 5. FRONTEND LOGIC (IMPORTANT DETAILS)
Disable Accept Button if:
task.status !== "open"
user is creator
Show correct UI per role:
Creator:
Approve / Reject buttons
Worker:
Submit form
Others:
View only
🎨 6. UI STACK (CLEAN SETUP)

Use:

Tailwind
shadcn/ui
Sonner (toasts)
Toast examples:
toast.success("Task accepted");
toast.success("Submission sent");
toast.success("Payment received");
toast.error("Something went wrong");
🌙 7. DARK MODE (quick setup)

Use next-themes or simple class toggle:

<html class="dark">
What is this?
📦 8. PROJECT STRUCTURE (WORKING)
/apps
  /web (React + Vite)
  /api (Express)

/packages
  /db (Drizzle schema)
🚀 9. DEPLOYMENT (KEEP SIMPLE)
Backend:
Deploy on Render / Railway
Frontend:
Deploy on Vercel
DB:
Neon (free)

Deploy + test
💡 FINAL STRATEGY (THIS IS THE DIFFERENCE)

Don’t launch this as:
❌ “task marketplace”

Launch it as:
🔥 “AI Content Job Board”

Examples on landing:

“Get viral reels made for ₹100”
“Hire AI editors instantly”
“Turn prompts into income”