PHASE 1 — MVP (GET IT WORKING FAST)

👉 Goal:
Users can post → accept → submit → approve → earn

✅ Build ONLY this first:
1. Auth (keep it simple)
Use Clerk (good choice)
Store:
id
email
name
balance = 0
2. Core Tables (simplified)

Don’t overcomplicate. Start with:

Users

id
clerkId
email
balance
pendingBalance

Tasks

id
title
description
budget
status (open, in_progress, submitted, completed)
creatorId
workerId

Submissions

id
taskId
content
status (pending, approved, rejected)

Transactions

id
userId
amount
type (earning, fee)
3. Core Flow (MOST IMPORTANT)

This is your money engine:

User creates task
→ budget stored in DB (escrow simulation)
Worker accepts task
→ status = in_progress
Worker submits work
→ status = submitted
Creator approves
→ THIS is where money happens:
Worker gets: 90%
Platform keeps: 10%
💰 Commission Logic (core of your app)

Use this logic:

Worker earning = budget * 0.9
Platform fee = budget * 0.1

Update:

worker.balance += earning
⚠️ SKIP THIS FOR NOW (Phase 2 later)

Don’t build yet:

Razorpay real payments
OpenAPI + codegen
Complex admin dashboards
Withdrawal system

👉 These slow you down early.

🚀 PHASE 2 — MAKE IT REAL MONEY

Once MVP works:

Add:

Razorpay payment (real escrow)
Withdrawal requests
Admin approval system
🏗️ BACKEND STRUCTURE (clean & scalable)

Since you're using:

Express + Drizzle → good choice

Structure like this:

/src
  /routes
    tasks.ts
    submissions.ts
    wallet.ts
    admin.ts
  /db
    schema.ts
  /lib
    auth.ts
    commission.ts
🔥 KEY API ENDPOINTS (don’t overbuild)
Tasks
POST   /tasks
GET    /tasks
POST   /tasks/:id/accept
POST   /tasks/:id/submit
POST   /tasks/:id/approve
POST   /tasks/:id/reject
Wallet
GET /wallet
Admin (later)
GET /admin/users
GET /admin/transactions
🎨 FRONTEND PAGES (MVP ONLY)

Start with just 5 pages:

/ → Landing
/tasks → Task feed
/tasks/[id] → Task detail
/create → Post task
/dashboard → Your tasks + earnings
⚡ CRITICAL UX DETAILS (this is what makes it feel real)
Disable "Accept Task" if already taken
Show task status clearly
Add loading states everywhere
Toast messages:
"Task accepted"
"Submission sent"
"Payment received"
💡 YOUR REAL ADVANTAGE (important)

Don’t make this generic.

👉 Niche it:

AI Content Tasks ONLY

Examples:

“Make viral reel like this”
“Generate 10 hooks”
“Create AI thumbnail”

This:

Reduces competition
Attracts your audience
Fits your experience
⚠️ HARD TRUTH

Your biggest risk is NOT coding.

It’s:

building something nobody uses

So early on:

Don’t perfect code
Test with real users
Even 5 users is enough
🧭 SIMPLE EXECUTION ROADMAP

Day 1–2:

Auth + DB + basic UI

Day 3–4:

Task system working

Day 5:

Submission + approval

Day 6:

Wallet logic

Day 7:

Deploy on Vercel